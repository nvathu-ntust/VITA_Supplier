<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>VITA - About Us</title>

    <!-- HTMX -->
    <script src="https://unpkg.com/htmx.org@1.9.12"></script>

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>

    <style>
        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --bg: #ffffff;
            --fg: #0f172a;
            --muted: #64748b;
            --muted-fg: #94a3b8;
            --border: #e2e8f0;
            --card: #ffffff;
            --secondary: #f8fafc;
            --radius: 0.5rem;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: system-ui, sans-serif;
            line-height: 1.6;
            color: var(--fg);
            background: var(--bg);
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        header {
            position: sticky;
            top: 0;
            z-index: 50;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border);
        }

        .header-inner {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 64px;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 800;
            font-size: 1.5rem;
        }

        nav {
            display: none;
            gap: 2rem;
            font-weight: 500;
        }

        nav a:hover {
            color: var(--primary);
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: var(--radius);
            font-weight: 600;
            cursor: pointer;
            transition: all .2s;
        }

        .btn-ghost:hover {
            background: var(--secondary);
        }

        .hero {
            position: relative;
            min-height: 60vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
            padding: 4rem 1rem;
        }

        .hero::before {
            content: "";
            position: absolute;
            inset: 0;
            z-index: -1;
            background: linear-gradient(to right, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.4)),
                url('/assets/about-hero.jpg') center/cover no-repeat;
        }

        .hero h1 {
            font-size: 3rem;
            font-weight: 900;
            margin-bottom: 1rem;
        }

        .hero p {
            font-size: 1.25rem;
            color: var(--muted-fg);
            max-width: 600px;
            margin: 0 auto;
        }

        .section {
            padding: 6rem 0;
            text-align: center;
        }

        .section-title {
            font-size: 2.8rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }

        .section-title span {
            color: var(--primary);
        }

        .section-subtitle {
            font-size: 1.25rem;
            color: var(--muted-fg);
            max-width: 700px;
            margin: 0 auto 4rem;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .card {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 2rem;
            transition: all .3s;
        }

        .card:hover {
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
            transform: translateY(-8px);
        }

        .card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: var(--radius);
            margin-bottom: 1rem;
        }

        .card h3 {
            font-size: 1.4rem;
            margin-bottom: 0.75rem;
            font-weight: 700;
        }

        .card p {
            color: var(--muted-fg);
            font-size: 0.95rem;
        }

        footer {
            background: var(--secondary);
            border-top: 1px solid var(--border);
            padding: 4rem 0 2rem;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 800;
            font-size: 1.4rem;
            margin-bottom: 1rem;
        }

        .footer-logo img {
            height: 32px;
            width: 32px;
        }

        .footer-col h4 {
            font-weight: 600;
            margin-bottom: 1.5rem;
        }

        .footer-col a {
            display: block;
            padding: 0.4rem 0;
            color: var(--muted-fg);
        }

        .footer-col a:hover {
            color: var(--primary);
        }

        .copyright {
            text-align: center;
            padding-top: 2rem;
            border-top: 1px solid var(--border);
            color: var(--muted-fg);
            font-size: 0.9rem;
        }

        @media (min-width: 768px) {
            nav {
                display: flex;
            }

            .hero h1 {
                font-size: 4rem;
            }
        }

        @media (min-width: 1024px) {
            .hero h1 {
                font-size: 4.5rem;
            }
        }
    </style>
</head>

<body>

    <!-- Header -->
    <header>
        <div class="container">
            <div class="header-inner">
                <div class="logo">
                    <img src="/assets/vita-logo.png" alt="VITA Logo">
                    <span>VITA</span>
                </div>
                <nav>
                    <a href="/">Home</a>
                    <a href="/about">About Us</a>
                    <a href="/service">Service</a>
                    <a href="/career">Career</a>
                    <a href="/contact">Contact</a>
                </nav>
                <div>
                    <% if (userName) { %>
                        <span>Welcome <%= userName %></span>
                        <% } else { %>
                            <button class="btn btn-ghost"><a href="/register">Sign Up</a></button>
                            <% } %>
                </div>
            </div>
        </div>
    </header>

    <!-- Hero -->
    <section class="hero">
        <div class="container">
            <h1>About <span>VITA</span></h1>
            <p>We empower general contractors to build better projects through efficient supplier management and
                data-driven insights.</p>
        </div>
    </section>

    <!-- Mission & Vision -->
    <section class="section">
        <h2 class="section-title">Our <span>Mission</span></h2>
        <p class="section-subtitle">To provide general contractors with an intuitive platform to manage suppliers,
            streamline orders, and ensure project success.</p>
    </section>

    <section class="section">
        <h2 class="section-title">Our <span>Vision</span></h2>
        <p class="section-subtitle">To be the leading platform for supplier management and analytics in the construction
            industry.</p>
    </section>

    <!-- Team -->
    <section class="section">
        <h2 class="section-title">Meet Our <span>Team</span></h2>
        <p class="section-subtitle">Dedicated professionals committed to building better tools for contractors.</p>

        <div class="grid">
            <% var team=[ {name:"Thu Nguyen", role:"CEO", photo:"/assets/team1.jpg"}, ]; %>

                <% team.forEach(member=> { %>
                    <div class="card">
                        <img src="<%= member.photo %>" alt="<%= member.name %>">
                        <h3>
                            <%= member.name %>
                        </h3>
                        <p>
                            <%= member.role %>
                        </p>
                    </div>
                    <% }); %>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-grid">
                <div>
                    <div class="footer-logo">
                        <img src="/assets/vita-logo.png" alt="VITA">
                        <span>VITA</span>
                    </div>
                    <p style="color:var(--muted-fg);max-width:300px;">
                        Empowering general contractors to build better through streamlined supplier management.
                    </p>
                </div>
                <div class="footer-col">
                    <h4>Product</h4>
                    <a href="#">Features</a><a href="#">Pricing</a><a href="#">Case Studies</a>
                </div>
                <div class="footer-col">
                    <h4>Company</h4>
                    <a href="#">About</a><a href="#">Blog</a><a href="#">Careers</a>
                </div>
                <div class="footer-col">
                    <h4>Support</h4>
                    <a href="#">Help Center</a><a href="#">Contact Us</a><a href="#">Privacy</a>
                </div>
            </div>
            <div class="copyright">© 2025 VITA. All rights reserved.</div>
        </div>
    </footer>

    <script>
        lucide.createIcons();
    </script>
</body>

</html>